# Dependencies
node_modules/

# Next.js
.next/
out/
next-env.d.ts

# Environment / secrets
.env
.env.local
.env.*.local

# Admin-uploaded media is written at runtime and not source. Uploads are named
# `file-<timestamp>.<ext>`, so ignoring that prefix keeps the bundled seed
# images (amir.png, project covers) tracked alongside them.
uploads/file-*

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
